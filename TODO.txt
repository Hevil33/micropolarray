test per tutte le classi con testing (assert fatto bene)
compressione dei files necessari (o temp_dir in cui poi cancellarli?)


cambiare operazioni su flattened arrays se possibile per risparmiare tempo
implementare lettura di tipi di files diversi dal fits


METTERE A POSTO I FIXTO
faster nrgf filter

passare tutti i dati a float

